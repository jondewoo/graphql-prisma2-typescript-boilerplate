# Migration `20191002224130-password`

This migration has been generated by Jonathan Derrough at 10/2/2019, 10:41:30 PM.
You can check out the [state of the datamodel](./datamodel.prisma) after the migration.

## Database Steps

```sql
DROP TABLE "lift"."Post";

PRAGMA foreign_keys=OFF;

CREATE TABLE "lift"."new_User" (
  "email" TEXT NOT NULL DEFAULT ''  ,
  "id" TEXT NOT NULL   ,
  "name" TEXT NOT NULL DEFAULT ''  ,
  "password" TEXT NOT NULL DEFAULT ''  ,
  PRIMARY KEY ("id")
);

INSERT INTO "new_User" ("email","id","name") SELECT "email","id","name" from "User"

DROP TABLE "lift"."User";

ALTER TABLE "lift"."new_User" RENAME TO "User";

CREATE UNIQUE INDEX "lift"."User.id" ON "User"("id")

CREATE UNIQUE INDEX "lift"."User.email" ON "User"("email")

PRAGMA "lift".foreign_key_check;

PRAGMA foreign_keys=ON;
```

## Changes

```diff
diff --git datamodel.mdl datamodel.mdl
migration 20191002144344-init..20191002224130-password
--- datamodel.dml
+++ datamodel.dml
@@ -13,17 +13,7 @@
 model User {
   id    String  @default(cuid()) @id @unique
   email String  @unique
-  name  String?
-  posts Post[]
+  name  String
+  password String
 }
-
-model Post {
-  id        String   @default(cuid()) @id @unique
-  createdAt DateTime @default(now())
-  updatedAt DateTime @updatedAt
-  published Boolean
-  title     String
-  content   String?
-  author    User?
-}
```

## Photon Usage

You can use a specific Photon built for this migration (20191002224130-password)
in your `before` or `after` migration script like this:

```ts
import Photon from '@generated/photon/20191002224130-password'

const photon = new Photon()

async function main() {
  const result = await photon.users()
  console.dir(result, { depth: null })
}

main()

```
